# 屏幕像素监控工具使用说明

## 简介

屏幕像素监控工具是一个可以实时监控屏幕特定位置或区域像素变化的应用。当检测到预设的像素颜色或区域变化时，会自动弹出图片并播放音频。

## 功能特点

1. **像素监控**：监控屏幕指定位置的像素颜色，匹配时触发
2. **区域监控**：监控屏幕指定区域的变化，当区域内像素变化超过阈值时触发
3. **弹窗显示**：触发时可在屏幕上显示自定义图片
4. **音频播放**：触发时可播放自定义音频文件
5. **画中画效果**：支持全屏或自定义大小的画中画显示
6. **可配置性**：通过JSON配置文件灵活设置监控规则

## 安装依赖

在使用前，请确保已安装所需的Python依赖：

```bash
pip install -r requirements.txt
```

## 配置文件说明

配置文件为JSON格式，主要包含以下部分：

```json
{
    "check_rate": 10,           // 检测频率（次/秒）
    "cooldown": 3000,           // 触发冷却时间（毫秒）
    "triggers": [              // 触发器列表
        {
            "name": "触发器名称",
            "type": "pixel",   // 或 "region"
            // 像素监控特有参数
            "position": [x, y], // 监控坐标
            "target_color": [r, g, b], // 目标颜色
            "tolerance": 10,    // 颜色容差

            // 区域监控特有参数
            "region": {         // 监控区域
                "left": x,
                "top": y,
                "width": w,
                "height": h
            },
            "tolerance": 30,     // 像素变化容差
            "change_threshold": 0.2, // 变化比例阈值

            // 通用参数
            "image_path": "path/to/image.png", // 图片路径
            "audio_path": "path/to/audio.wav", // 音频路径
            "display_duration": 2000,          // 显示持续时间（毫秒）

            // 画中画配置
            "pip": {
                "position": [x, y], // 显示位置
                "size": [w, h],    // 显示尺寸
                "fullscreen": true, // 是否全屏
                "alpha": 0.7       // 透明度
            }
        }
    ]
}
```

## 使用方法

1. 准备配置文件（如 `config.json` 或使用提供的 `example_config.json`）
2. 运行主程序：
   ```bash
   python screen_monitor.py [配置文件路径]
   ```
   如果不指定配置文件路径，默认使用 `config.json`

3. 在弹出的界面中点击"开始监控"按钮
4. 程序会在后台检测屏幕变化，当满足触发条件时自动显示图片和播放音频

## 工具说明

项目还包含以下辅助工具：

- **pixel_picker.py**：屏幕像素坐标拾取工具，可以帮助获取需要监控的屏幕坐标
- **region_selector.py**：屏幕区域框选工具，可以帮助选择需要监控的区域

## 示例场景

1. **游戏击杀提示**：监控游戏中特定位置的颜色变化，当检测到击杀提示时弹出图片并播放音效
2. **屏幕变化提醒**：监控某个区域的变化，当检测到变化时提醒用户
3. **自动化测试**：用于UI自动化测试，检测界面元素是否出现

## 注意事项

1. 程序需要足够的权限才能截取屏幕内容
2. 检测频率过高可能会影响系统性能
3. 全屏显示时，图片会覆盖整个屏幕，但支持鼠标穿透
